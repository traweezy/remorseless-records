diff --git a/index.js b/index.js
index 901f2e2..dbf77a9 100644
--- a/index.js
+++ b/index.js
@@ -9,6 +9,11 @@ const yauzl = require('yauzl')
 const openZip = promisify(yauzl.open)
 const pipeline = promisify(stream.pipeline)
-
+
+const isPathOutsideRoot = (root, candidate) => {
+  const relative = path.relative(root, candidate)
+  return relative === '..' || relative.startsWith(`..${path.sep}`) || path.isAbsolute(relative)
+}
+
 class Extractor {
   constructor (zipPath, opts) {
     this.zipPath = zipPath
@@ -49,7 +54,6 @@ class Extractor {
           const canonicalDestDir = await fs.realpath(destDir)
-          const relativeDestDir = path.relative(this.opts.dir, canonicalDestDir)
-
+
-          if (relativeDestDir.split(path.sep).includes('..')) {
+          if (isPathOutsideRoot(this.opts.dir, canonicalDestDir)) {
             throw new Error(`Out of bound path "${canonicalDestDir}" found while processing file ${entry.fileName}`)
           }
-
+
@@ -110,8 +115,13 @@ class Extractor {
-
+
     if (symlink) {
-      const link = await getStream(readStream)
+      const link = (await getStream(readStream)).toString()
+      const linkTarget = path.resolve(path.dirname(dest), link)
+      if (isPathOutsideRoot(this.opts.dir, linkTarget)) {
+        throw new Error(`Out of bound symlink target "${link}" found while processing file ${entry.fileName}`)
+      }
+
       debug('creating symlink', link, dest)
       await fs.symlink(link, dest)
     } else {
       await pipeline(readStream, createWriteStream(dest, { mode: procMode }))
     }
