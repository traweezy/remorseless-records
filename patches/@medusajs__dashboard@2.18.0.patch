diff --git a/dist/app.js b/dist/app.js
index c954aa5b0d99edecab81c0da09c3dbc5ba2a652c..483de42137568d9bb22e3586ea14eaa4cf438fc8 100644
--- a/dist/app.js
+++ b/dist/app.js
@@ -111811,17 +111811,25 @@ var init_layout_composer = __esm({
           widgetsZonePrefix,
           layout?.sections?.map((s) => s.id) ?? []
         );
-        const rawEntries = buildCoreEntries(coreElementsBySection);
+        const beforeWidgets = [];
+        const afterWidgets = [];
         for (const [naturalSection, widgets] of Object.entries(naturalWidgets)) {
           for (const w of widgets) {
             const WidgetComponent = w.Component;
-            rawEntries.push({
+            const entry = {
               widgetId: w.widgetId,
               render: (data2) => /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(WidgetComponent, { data: data2 }),
               naturalSection
-            });
+            };
+            const target = w.placement === "before" ? beforeWidgets : afterWidgets;
+            target.push(entry);
           }
         }
+        const rawEntries = [
+          ...beforeWidgets,
+          ...buildCoreEntries(coreElementsBySection),
+          ...afterWidgets
+        ];
         const validSectionIds2 = new Set(layout?.sections.map((s) => s.id) ?? []);
         const entriesBySection2 = buildDisplayEntries(
           rawEntries,
@@ -113123,7 +113131,7 @@ var init_action_menu = __esm({
       children
     }) => {
       const direction = useDocumentDirection();
-      const inner = children ?? /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(import_ui17.IconButton, { size: "small", variant, children: /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(import_icons10.EllipsisHorizontal, {}) });
+      const inner = children ?? /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(import_ui17.IconButton, { "aria-label": "Open actions", size: "small", variant, children: /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(import_icons10.EllipsisHorizontal, {}) });
       return /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)(import_ui17.DropdownMenu, { dir: direction, children: [
         /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(import_ui17.DropdownMenu.Trigger, { asChild: true, children: inner }),
         /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(import_ui17.DropdownMenu.Content, { children: groups.map((group, index) => {
@@ -116319,7 +116327,7 @@ var init_data_table_order_by = __esm({
         });
       };
       return /* @__PURE__ */ (0, import_jsx_runtime50.jsxs)(import_ui33.DropdownMenu, { dir: direction, children: [
-        /* @__PURE__ */ (0, import_jsx_runtime50.jsx)(import_ui33.DropdownMenu.Trigger, { asChild: true, children: /* @__PURE__ */ (0, import_jsx_runtime50.jsx)(import_ui33.IconButton, { size: "small", children: /* @__PURE__ */ (0, import_jsx_runtime50.jsx)(import_icons24.DescendingSorting, {}) }) }),
+        /* @__PURE__ */ (0, import_jsx_runtime50.jsx)(import_ui33.DropdownMenu.Trigger, { asChild: true, children: /* @__PURE__ */ (0, import_jsx_runtime50.jsx)(import_ui33.IconButton, { "aria-label": "Sort results", size: "small", children: /* @__PURE__ */ (0, import_jsx_runtime50.jsx)(import_icons24.DescendingSorting, {}) }) }),
         /* @__PURE__ */ (0, import_jsx_runtime50.jsxs)(import_ui33.DropdownMenu.Content, { className: "z-[1]", align: "end", children: [
           /* @__PURE__ */ (0, import_jsx_runtime50.jsx)(
             import_ui33.DropdownMenu.RadioGroup,
@@ -116431,6 +116439,7 @@ var init_data_table_search = __esm({
       return /* @__PURE__ */ (0, import_jsx_runtime51.jsx)(
         import_ui34.Input,
         {
+          "aria-label": placeholderText,
           autoComplete: "off",
           name: "q",
           type: "search",
@@ -116634,7 +116643,7 @@ var init_data_table_root = __esm({
                             cell.getContext()
                           );
                           const isTabableLink = isFirstCell && !!to;
-                          const shouldRenderAsLink = !!to && !isSelectCell;
+                          const shouldRenderAsLink = !!to && !isSelectCell && cell.column.id !== "actions";
                           return /* @__PURE__ */ (0, import_jsx_runtime53.jsx)(
                             import_ui35.Table.Cell,
                             {
@@ -116865,36 +116874,7 @@ var init_product_list_table_actions = __esm({
       product
     }) => {
       const { t: t5 } = (0, import_react_i18next32.useTranslation)();
-      const prompt = (0, import_ui37.usePrompt)();
-      const { mutateAsync } = useDeleteProduct(product.id);
       const isTranslationsEnabled = useFeatureFlag("translation");
-      const handleDelete = async () => {
-        const res = await prompt({
-          title: t5("general.areYouSure"),
-          description: t5("products.deleteWarning", {
-            title: product.title
-          }),
-          confirmText: t5("actions.delete"),
-          cancelText: t5("actions.cancel")
-        });
-        if (!res) {
-          return;
-        }
-        await mutateAsync(void 0, {
-          onSuccess: () => {
-            import_ui37.toast.success(t5("products.toasts.delete.success.header"), {
-              description: t5("products.toasts.delete.success.description", {
-                title: product.title
-              })
-            });
-          },
-          onError: (e) => {
-            import_ui37.toast.error(t5("products.toasts.delete.error.header"), {
-              description: e.message
-            });
-          }
-        });
-      };
       return /* @__PURE__ */ (0, import_jsx_runtime55.jsx)(
         ActionMenu,
         {
@@ -116918,16 +116898,7 @@ var init_product_list_table_actions = __esm({
                   }
                 ]
               }
-            ] : [],
-            {
-              actions: [
-                {
-                  icon: /* @__PURE__ */ (0, import_jsx_runtime55.jsx)(import_icons25.Trash, {}),
-                  label: t5("actions.delete"),
-                  onClick: handleDelete
-                }
-              ]
-            }
+            ] : []
           ]
         }
       );
@@ -117626,7 +117597,6 @@ var init_product_list_table = __esm({
           /* @__PURE__ */ (0, import_jsx_runtime65.jsx)(import_ui41.Heading, { level: "h1", children: t5("products.domain") }),
           /* @__PURE__ */ (0, import_jsx_runtime65.jsxs)("div", { className: "flex items-center justify-center gap-x-2", children: [
             /* @__PURE__ */ (0, import_jsx_runtime65.jsx)(import_ui41.Button, { size: "small", variant: "secondary", asChild: true, children: /* @__PURE__ */ (0, import_jsx_runtime65.jsx)(import_react_router_dom26.Link, { to: `export${location.search}`, children: t5("actions.export") }) }),
-            /* @__PURE__ */ (0, import_jsx_runtime65.jsx)(import_ui41.Button, { size: "small", variant: "secondary", asChild: true, children: /* @__PURE__ */ (0, import_jsx_runtime65.jsx)(import_react_router_dom26.Link, { to: `import${location.search}`, children: t5("actions.import") }) }),
             /* @__PURE__ */ (0, import_jsx_runtime65.jsx)(import_ui41.Button, { size: "small", variant: "secondary", asChild: true, children: /* @__PURE__ */ (0, import_jsx_runtime65.jsx)(import_react_router_dom26.Link, { to: "create", children: t5("actions.create") }) })
           ] })
         ] }),
@@ -117664,6 +117634,7 @@ var init_product_list_table = __esm({
           ...base,
           columnHelper2.display({
             id: "actions",
+            header: () => /* @__PURE__ */ (0, import_jsx_runtime65.jsx)("span", { className: "sr-only", children: "Actions" }),
             cell: ({ row }) => {
               return /* @__PURE__ */ (0, import_jsx_runtime65.jsx)(ProductActions, { product: row.original });
             }
@@ -123459,7 +123430,6 @@ var init_configurable_product_list_table = __esm({
             heading: t5("products.domain"),
             actions: [
               { label: t5("actions.export"), to: `export${location.search}` },
-              { label: t5("actions.import"), to: `import${location.search}` },
               { label: t5("actions.create"), to: "create" }
             ]
           }
@@ -134419,35 +134389,9 @@ var init_product_general_section = __esm({
       product
     }) => {
       const { t: t5 } = (0, import_react_i18next88.useTranslation)();
-      const prompt = (0, import_ui99.usePrompt)();
-      const navigate = (0, import_react_router_dom35.useNavigate)();
       const { getDisplays } = useExtension();
       const isTranslationsEnabled = useFeatureFlag("translation");
       const displays = getDisplays("product", "general");
-      const { mutateAsync } = useDeleteProduct(product.id);
-      const handleDelete = async () => {
-        const res = await prompt({
-          title: t5("general.areYouSure"),
-          description: t5("products.deleteWarning", {
-            title: product.title
-          }),
-          confirmText: t5("actions.delete"),
-          cancelText: t5("actions.cancel")
-        });
-        if (!res) {
-          return;
-        }
-        await mutateAsync(void 0, {
-          onSuccess: () => {
-            navigate("..");
-          },
-          onError: (e) => {
-            import_ui99.toast.error(t5("products.toasts.delete.error.header"), {
-              description: e.message
-            });
-          }
-        });
-      };
       return /* @__PURE__ */ (0, import_jsx_runtime136.jsxs)(import_ui99.Container, { className: "divide-y p-0", children: [
         /* @__PURE__ */ (0, import_jsx_runtime136.jsxs)("div", { className: "flex items-center justify-between px-6 py-4", children: [
           /* @__PURE__ */ (0, import_jsx_runtime136.jsx)(import_ui99.Heading, { children: product.title }),
@@ -134476,16 +134420,7 @@ var init_product_general_section = __esm({
                         }
                       ]
                     }
-                  ] : [],
-                  {
-                    actions: [
-                      {
-                        label: t5("actions.delete"),
-                        onClick: handleDelete,
-                        icon: /* @__PURE__ */ (0, import_jsx_runtime136.jsx)(import_icons45.Trash, {})
-                      }
-                    ]
-                  }
+                  ] : []
                 ]
               }
             )
@@ -135147,8 +135082,6 @@ var init_product_variant_section = __esm({
     useColumns4 = (product) => {
       const { t: t5 } = (0, import_react_i18next93.useTranslation)();
       const navigate = (0, import_react_router_dom38.useNavigate)();
-      const { mutateAsync } = useDeleteVariantLazy(product.id);
-      const prompt = (0, import_ui104.usePrompt)();
       const [searchParams] = (0, import_react_router_dom38.useSearchParams)();
       const tableSearchParams = (0, import_react140.useMemo)(() => {
         const filtered = new URLSearchParams();
@@ -135159,23 +135092,6 @@ var init_product_variant_section = __esm({
         }
         return filtered;
       }, [searchParams]);
-      const handleDelete = (0, import_react140.useCallback)(
-        async (id2, title) => {
-          const res = await prompt({
-            title: t5("general.areYouSure"),
-            description: t5("products.deleteVariantWarning", {
-              title
-            }),
-            confirmText: t5("actions.delete"),
-            cancelText: t5("actions.cancel")
-          });
-          if (!res) {
-            return;
-          }
-          await mutateAsync({ variantId: id2 });
-        },
-        [mutateAsync, prompt, t5]
-      );
       const optionColumns = (0, import_react140.useMemo)(() => {
         if (!product?.options) {
           return [];
@@ -135232,13 +135148,6 @@ var init_product_variant_section = __esm({
               }
             }
           ];
-          const secondaryActions = [
-            {
-              icon: /* @__PURE__ */ (0, import_jsx_runtime141.jsx)(import_icons50.Trash, {}),
-              label: t5("actions.delete"),
-              onClick: () => handleDelete(variant.id, variant.title)
-            }
-          ];
           const inventoryItemsCount = variant.inventory_items?.length || 0;
           switch (inventoryItemsCount) {
             case 0:
@@ -135271,9 +135180,9 @@ var init_product_variant_section = __esm({
               });
             }
           }
-          return [mainActions, secondaryActions];
+          return [mainActions];
         },
-        [handleDelete, navigate, t5, tableSearchParams]
+        [navigate, t5, tableSearchParams]
       );
       const getInventory = (0, import_react140.useCallback)(
         (variant) => {
@@ -141169,29 +141078,8 @@ var init_loader5 = __esm({
 // src/routes/product-variants/product-variant-detail/components/variant-general-section/variant-general-section.tsx
 function VariantGeneralSection({ variant }) {
   const { t: t5 } = (0, import_react_i18next124.useTranslation)();
-  const prompt = (0, import_ui133.usePrompt)();
-  const navigate = (0, import_react_router_dom58.useNavigate)();
   const isTranslationsEnabled = useFeatureFlag("translation");
   const hasInventoryKit = (variant.inventory?.length ?? 0) > 1;
-  const { mutateAsync } = useDeleteVariant(variant.product_id, variant.id);
-  const handleDelete = async () => {
-    const res = await prompt({
-      title: t5("general.areYouSure"),
-      description: t5("products.variant.deleteWarning", {
-        title: variant.title
-      }),
-      confirmText: t5("actions.delete"),
-      cancelText: t5("actions.cancel")
-    });
-    if (!res) {
-      return;
-    }
-    await mutateAsync(void 0, {
-      onSuccess: () => {
-        navigate("..", { replace: true });
-      }
-    });
-  };
   return /* @__PURE__ */ (0, import_jsx_runtime183.jsxs)(import_ui133.Container, { className: "divide-y p-0", children: [
     /* @__PURE__ */ (0, import_jsx_runtime183.jsxs)("div", { className: "flex items-center justify-between px-6 py-4", children: [
       /* @__PURE__ */ (0, import_jsx_runtime183.jsxs)("div", { children: [
@@ -141224,16 +141112,7 @@ function VariantGeneralSection({ variant }) {
                   }
                 ]
               }
-            ] : [],
-            {
-              actions: [
-                {
-                  label: t5("actions.delete"),
-                  onClick: handleDelete,
-                  icon: /* @__PURE__ */ (0, import_jsx_runtime183.jsx)(import_icons59.Trash, {})
-                }
-              ]
-            }
+            ] : []
           ]
         }
       ) })
@@ -218597,14 +218476,6 @@ function getRouteMap({
                   path: "",
                   lazy: () => Promise.resolve().then(() => (init_product_list2(), product_list_exports)),
                   children: [
-                    {
-                      path: "create",
-                      lazy: () => Promise.resolve().then(() => (init_product_create2(), product_create_exports))
-                    },
-                    {
-                      path: "import",
-                      lazy: () => Promise.resolve().then(() => (init_product_import2(), product_import_exports))
-                    },
                     {
                       path: "export",
                       lazy: () => Promise.resolve().then(() => (init_product_export2(), product_export_exports))
@@ -221160,9 +221031,10 @@ var init_dashboard_app = __esm({
       }
       /**
        * Returns widgets for a given base zone grouped by layout section.
-       * The legacy `.before`/`.after` ordering suffixes are stripped and
-       * ignored. Widgets are returned inregistration order and will be
-       * assigned a default order of 0 in the LayoutComposer.
+       * The legacy `.before`/`.after` ordering suffixes define the widget's
+       * natural placement around core entries. Widgets are returned in
+       * registration order and will be assigned a default order of 0 in the
+       * LayoutComposer.
        */
       getWidgetsForSections(route, sections) {
         const result = {};
@@ -221170,7 +221042,9 @@ var init_dashboard_app = __esm({
           if (!zone.startsWith(route)) {
             continue;
           }
-          const suffix = zone.slice(route.length + 1).replace(/\.?(before|after)$/, "");
+          const zoneSuffix = zone.slice(route.length + 1);
+          const placement = zoneSuffix.endsWith("before") ? "before" : "after";
+          const suffix = zoneSuffix.replace(/\.?(before|after)$/, "");
           const mainSection = sections.includes("main") ? "main" : sections[0];
           const section = suffix || mainSection;
           if (!result[section]) {
@@ -221180,6 +221054,7 @@ var init_dashboard_app = __esm({
             const sourceId = ext.widgetId ?? `${zone}:${i}`;
             result[section].push({
               Component: ext.Component,
+              placement,
               // The id intentionally omits the section so a widget keeps its
               // identity (and saved preference) when its zone moves to a different
               // section of the same route.
diff --git a/dist/chunk-7TWTWTDT.mjs b/dist/chunk-7TWTWTDT.mjs
index 5cd0631a4956c01cd1b2cfb9884ba5fc872bdc46..6c4fda8951dfc7ef93f46a44525f6b215fdb1dfc 100644
--- a/dist/chunk-7TWTWTDT.mjs
+++ b/dist/chunk-7TWTWTDT.mjs
@@ -55,6 +55,7 @@ var DataTableSearch = ({
   return /* @__PURE__ */ jsx(
     Input,
     {
+      "aria-label": placeholderText,
       autoComplete: "off",
       name: "q",
       type: "search",
@@ -233,7 +234,7 @@ var DataTableRoot = ({
                         cell.getContext()
                       );
                       const isTabableLink = isFirstCell && !!to;
-                      const shouldRenderAsLink = !!to && !isSelectCell;
+                      const shouldRenderAsLink = !!to && !isSelectCell && cell.column.id !== "actions";
                       return /* @__PURE__ */ jsx3(
                         Table.Cell,
                         {
diff --git a/dist/chunk-HQKGZADC.mjs b/dist/chunk-HQKGZADC.mjs
index dc03eeee9046413b7ce3d07eb2c2849dfa3ebd37..ca288289d16d24234350d67080f15ef81529262a 100644
--- a/dist/chunk-HQKGZADC.mjs
+++ b/dist/chunk-HQKGZADC.mjs
@@ -68,7 +68,7 @@ var DataTableOrderBy = ({
     });
   };
   return /* @__PURE__ */ jsxs(DropdownMenu, { dir: direction, children: [
-    /* @__PURE__ */ jsx(DropdownMenu.Trigger, { asChild: true, children: /* @__PURE__ */ jsx(IconButton, { size: "small", children: /* @__PURE__ */ jsx(DescendingSorting, {}) }) }),
+    /* @__PURE__ */ jsx(DropdownMenu.Trigger, { asChild: true, children: /* @__PURE__ */ jsx(IconButton, { "aria-label": "Sort results", size: "small", children: /* @__PURE__ */ jsx(DescendingSorting, {}) }) }),
     /* @__PURE__ */ jsxs(DropdownMenu.Content, { className: "z-[1]", align: "end", children: [
       /* @__PURE__ */ jsx(
         DropdownMenu.RadioGroup,
diff --git a/dist/chunk-OZPB6JBL.mjs b/dist/chunk-OZPB6JBL.mjs
index 05649d580083c5d28a1d9c75c8907e6b7e655c96..f735fd72ae22e34e92ce52c53e703134f50771c2 100644
--- a/dist/chunk-OZPB6JBL.mjs
+++ b/dist/chunk-OZPB6JBL.mjs
@@ -16,7 +16,7 @@ var ActionMenu = ({
   children
 }) => {
   const direction = useDocumentDirection();
-  const inner = children ?? /* @__PURE__ */ jsx(IconButton, { size: "small", variant, children: /* @__PURE__ */ jsx(EllipsisHorizontal, {}) });
+  const inner = children ?? /* @__PURE__ */ jsx(IconButton, { "aria-label": "Open actions", size: "small", variant, children: /* @__PURE__ */ jsx(EllipsisHorizontal, {}) });
   return /* @__PURE__ */ jsxs(DropdownMenu, { dir: direction, children: [
     /* @__PURE__ */ jsx(DropdownMenu.Trigger, { asChild: true, children: inner }),
     /* @__PURE__ */ jsx(DropdownMenu.Content, { children: groups.map((group, index) => {
diff --git a/dist/chunk-QIJSUXW3.mjs b/dist/chunk-QIJSUXW3.mjs
index 1f0ae3f83bfd827fe091e934bbcb62337dc0f998..b4ad11f79eddb01888148f6c3453c2fa72de7c06 100644
--- a/dist/chunk-QIJSUXW3.mjs
+++ b/dist/chunk-QIJSUXW3.mjs
@@ -107502,14 +107502,6 @@ function getRouteMap({
                   path: "",
                   lazy: () => import("./product-list-KFP2GKHN.mjs"),
                   children: [
-                    {
-                      path: "create",
-                      lazy: () => import("./product-create-USIRFWXQ.mjs")
-                    },
-                    {
-                      path: "import",
-                      lazy: () => import("./product-import-ZIP6MEEQ.mjs")
-                    },
                     {
                       path: "export",
                       lazy: () => import("./product-export-GZQF56AL.mjs")
@@ -109943,9 +109935,10 @@ var DashboardApp = class {
   }
   /**
    * Returns widgets for a given base zone grouped by layout section.
-   * The legacy `.before`/`.after` ordering suffixes are stripped and
-   * ignored. Widgets are returned inregistration order and will be
-   * assigned a default order of 0 in the LayoutComposer.
+   * The legacy `.before`/`.after` ordering suffixes define the widget's
+   * natural placement around core entries. Widgets are returned in
+   * registration order and will be assigned a default order of 0 in the
+   * LayoutComposer.
    */
   getWidgetsForSections(route, sections) {
     const result = {};
@@ -109953,7 +109946,9 @@ var DashboardApp = class {
       if (!zone.startsWith(route)) {
         continue;
       }
-      const suffix = zone.slice(route.length + 1).replace(/\.?(before|after)$/, "");
+      const zoneSuffix = zone.slice(route.length + 1);
+      const placement = zoneSuffix.endsWith("before") ? "before" : "after";
+      const suffix = zoneSuffix.replace(/\.?(before|after)$/, "");
       const mainSection = sections.includes("main") ? "main" : sections[0];
       const section = suffix || mainSection;
       if (!result[section]) {
@@ -109963,6 +109958,7 @@ var DashboardApp = class {
         const sourceId = ext.widgetId ?? `${zone}:${i}`;
         result[section].push({
           Component: ext.Component,
+          placement,
           // The id intentionally omits the section so a widget keeps its
           // identity (and saved preference) when its zone moves to a different
           // section of the same route.
diff --git a/dist/chunk-Z3OGJXAM.mjs b/dist/chunk-Z3OGJXAM.mjs
index 3e5230d1d9e765aad336a7ada8ee7de59bfcc5d1..ac9ffd7ecc1f133c63a087d32adab614eac7cfe3 100644
--- a/dist/chunk-Z3OGJXAM.mjs
+++ b/dist/chunk-Z3OGJXAM.mjs
@@ -739,17 +739,25 @@ var LayoutComposerRoot = ({
       widgetsZonePrefix,
       layout?.sections?.map((s) => s.id) ?? []
     );
-    const rawEntries = buildCoreEntries(coreElementsBySection);
+    const beforeWidgets = [];
+    const afterWidgets = [];
     for (const [naturalSection, widgets] of Object.entries(naturalWidgets)) {
       for (const w of widgets) {
         const WidgetComponent = w.Component;
-        rawEntries.push({
+        const entry = {
           widgetId: w.widgetId,
           render: (data2) => /* @__PURE__ */ jsx4(WidgetComponent, { data: data2 }),
           naturalSection
-        });
+        };
+        const target = w.placement === "before" ? beforeWidgets : afterWidgets;
+        target.push(entry);
       }
     }
+    const rawEntries = [
+      ...beforeWidgets,
+      ...buildCoreEntries(coreElementsBySection),
+      ...afterWidgets
+    ];
     const validSectionIds2 = new Set(layout?.sections.map((s) => s.id) ?? []);
     const entriesBySection2 = buildDisplayEntries(
       rawEntries,
diff --git a/dist/components.js b/dist/components.js
index c5e00d8ee8258b0a64c95975948c21402ec98ce5..0cca0c2a3fb2a179fdda9b216a090b02e04e8256 100644
--- a/dist/components.js
+++ b/dist/components.js
@@ -784,17 +784,25 @@ var LayoutComposerRoot = ({
       widgetsZonePrefix,
       layout?.sections?.map((s) => s.id) ?? []
     );
-    const rawEntries = buildCoreEntries(coreElementsBySection);
+    const beforeWidgets = [];
+    const afterWidgets = [];
     for (const [naturalSection, widgets] of Object.entries(naturalWidgets)) {
       for (const w of widgets) {
         const WidgetComponent = w.Component;
-        rawEntries.push({
+        const entry = {
           widgetId: w.widgetId,
           render: (data2) => /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(WidgetComponent, { data: data2 }),
           naturalSection
-        });
+        };
+        const target = w.placement === "before" ? beforeWidgets : afterWidgets;
+        target.push(entry);
       }
     }
+    const rawEntries = [
+      ...beforeWidgets,
+      ...buildCoreEntries(coreElementsBySection),
+      ...afterWidgets
+    ];
     const validSectionIds2 = new Set(layout?.sections.map((s) => s.id) ?? []);
     const entriesBySection2 = buildDisplayEntries(
       rawEntries,
diff --git a/dist/product-detail-7UDXNUGN.mjs b/dist/product-detail-7UDXNUGN.mjs
index a22703f8d12bb735ac61a3d63cf54bd3dc2eaa7a..5c90bfd6682ba45d93d4e11168c68e41bbb7fbe5 100644
--- a/dist/product-detail-7UDXNUGN.mjs
+++ b/dist/product-detail-7UDXNUGN.mjs
@@ -87,8 +87,6 @@ import {
 import "./chunk-D3TDNKSZ.mjs";
 import {
   productsQueryKeys,
-  useDeleteProduct,
-  useDeleteVariantLazy,
   useProduct,
   useProductVariants,
   useUpdateProduct
@@ -201,10 +199,9 @@ var ProductAttributeSection = ({
 };
 
 // src/routes/products/product-detail/components/product-general-section/product-general-section.tsx
-import { GlobeEurope, PencilSquare as PencilSquare2, Trash } from "@medusajs/icons";
-import { Container as Container2, Heading as Heading2, StatusBadge, toast, usePrompt } from "@medusajs/ui";
+import { GlobeEurope, PencilSquare as PencilSquare2 } from "@medusajs/icons";
+import { Container as Container2, Heading as Heading2, StatusBadge } from "@medusajs/ui";
 import { useTranslation as useTranslation2 } from "react-i18next";
-import { useNavigate } from "react-router-dom";
 import { jsx as jsx3, jsxs as jsxs2 } from "react/jsx-runtime";
 var productStatusColor = (status) => {
   switch (status) {
@@ -224,35 +221,9 @@ var ProductGeneralSection = ({
   product
 }) => {
   const { t } = useTranslation2();
-  const prompt = usePrompt();
-  const navigate = useNavigate();
   const { getDisplays } = useExtension();
   const isTranslationsEnabled = useFeatureFlag("translation");
   const displays = getDisplays("product", "general");
-  const { mutateAsync } = useDeleteProduct(product.id);
-  const handleDelete = async () => {
-    const res = await prompt({
-      title: t("general.areYouSure"),
-      description: t("products.deleteWarning", {
-        title: product.title
-      }),
-      confirmText: t("actions.delete"),
-      cancelText: t("actions.cancel")
-    });
-    if (!res) {
-      return;
-    }
-    await mutateAsync(void 0, {
-      onSuccess: () => {
-        navigate("..");
-      },
-      onError: (e) => {
-        toast.error(t("products.toasts.delete.error.header"), {
-          description: e.message
-        });
-      }
-    });
-  };
   return /* @__PURE__ */ jsxs2(Container2, { className: "divide-y p-0", children: [
     /* @__PURE__ */ jsxs2("div", { className: "flex items-center justify-between px-6 py-4", children: [
       /* @__PURE__ */ jsx3(Heading2, { children: product.title }),
@@ -281,16 +252,7 @@ var ProductGeneralSection = ({
                     }
                   ]
                 }
-              ] : [],
-              {
-                actions: [
-                  {
-                    label: t("actions.delete"),
-                    onClick: handleDelete,
-                    icon: /* @__PURE__ */ jsx3(Trash, {})
-                  }
-                ]
-              }
+              ] : []
             ]
           }
         )
@@ -767,8 +729,7 @@ import {
   Buildings,
   Component,
   GlobeEurope as GlobeEurope2,
-  PencilSquare as PencilSquare7,
-  Trash as Trash2
+  PencilSquare as PencilSquare7
 } from "@medusajs/icons";
 import {
   Badge as Badge3,
@@ -777,8 +738,7 @@ import {
   createDataTableColumnHelper,
   createDataTableCommandHelper,
   createDataTableFilterHelper,
-  Tooltip as Tooltip4,
-  usePrompt as usePrompt3
+  Tooltip as Tooltip4
 } from "@medusajs/ui";
 import { keepPreviousData } from "@tanstack/react-query";
 import { useCallback, useMemo } from "react";
@@ -882,8 +842,6 @@ var columnHelper = createDataTableColumnHelper();
 var useColumns = (product) => {
   const { t } = useTranslation7();
   const navigate = useNavigate3();
-  const { mutateAsync } = useDeleteVariantLazy(product.id);
-  const prompt = usePrompt3();
   const [searchParams] = useSearchParams();
   const tableSearchParams = useMemo(() => {
     const filtered = new URLSearchParams();
@@ -894,23 +852,6 @@ var useColumns = (product) => {
     }
     return filtered;
   }, [searchParams]);
-  const handleDelete = useCallback(
-    async (id, title) => {
-      const res = await prompt({
-        title: t("general.areYouSure"),
-        description: t("products.deleteVariantWarning", {
-          title
-        }),
-        confirmText: t("actions.delete"),
-        cancelText: t("actions.cancel")
-      });
-      if (!res) {
-        return;
-      }
-      await mutateAsync({ variantId: id });
-    },
-    [mutateAsync, prompt, t]
-  );
   const optionColumns = useMemo(() => {
     if (!product?.options) {
       return [];
@@ -967,13 +908,6 @@ var useColumns = (product) => {
           }
         }
       ];
-      const secondaryActions = [
-        {
-          icon: /* @__PURE__ */ jsx8(Trash2, {}),
-          label: t("actions.delete"),
-          onClick: () => handleDelete(variant.id, variant.title)
-        }
-      ];
       const inventoryItemsCount = variant.inventory_items?.length || 0;
       switch (inventoryItemsCount) {
         case 0:
@@ -1006,9 +940,9 @@ var useColumns = (product) => {
           });
         }
       }
-      return [mainActions, secondaryActions];
+      return [mainActions];
     },
-    [handleDelete, navigate, t, tableSearchParams]
+    [navigate, t, tableSearchParams]
   );
   const getInventory = useCallback(
     (variant) => {
diff --git a/dist/product-list-KFP2GKHN.mjs b/dist/product-list-KFP2GKHN.mjs
index 28522b73fefac055a0ab3031851587f77e65e7d8..2cd31c7e8cf3c4acc751721c82d4155fff327dc0 100644
--- a/dist/product-list-KFP2GKHN.mjs
+++ b/dist/product-list-KFP2GKHN.mjs
@@ -84,7 +84,6 @@ import "./chunk-LYZ7OUA3.mjs";
 import "./chunk-D3TDNKSZ.mjs";
 import {
   productsQueryKeys,
-  useDeleteProduct,
   useProducts
 } from "./chunk-TPANFD43.mjs";
 import "./chunk-7AXHHXCX.mjs";
@@ -132,44 +131,14 @@ import { useTranslation as useTranslation2 } from "react-i18next";
 import { Link, Outlet, useLoaderData, useLocation } from "react-router-dom";
 
 // src/routes/products/product-list/components/product-list-table/product-list-table-actions.tsx
-import { GlobeEurope, PencilSquare, Trash } from "@medusajs/icons";
-import { toast, usePrompt } from "@medusajs/ui";
+import { GlobeEurope, PencilSquare } from "@medusajs/icons";
 import { useTranslation } from "react-i18next";
 import { jsx } from "react/jsx-runtime";
 var ProductActions = ({
   product
 }) => {
   const { t } = useTranslation();
-  const prompt = usePrompt();
-  const { mutateAsync } = useDeleteProduct(product.id);
   const isTranslationsEnabled = useFeatureFlag("translation");
-  const handleDelete = async () => {
-    const res = await prompt({
-      title: t("general.areYouSure"),
-      description: t("products.deleteWarning", {
-        title: product.title
-      }),
-      confirmText: t("actions.delete"),
-      cancelText: t("actions.cancel")
-    });
-    if (!res) {
-      return;
-    }
-    await mutateAsync(void 0, {
-      onSuccess: () => {
-        toast.success(t("products.toasts.delete.success.header"), {
-          description: t("products.toasts.delete.success.description", {
-            title: product.title
-          })
-        });
-      },
-      onError: (e) => {
-        toast.error(t("products.toasts.delete.error.header"), {
-          description: e.message
-        });
-      }
-    });
-  };
   return /* @__PURE__ */ jsx(
     ActionMenu,
     {
@@ -193,16 +162,7 @@ var ProductActions = ({
               }
             ]
           }
-        ] : [],
-        {
-          actions: [
-            {
-              icon: /* @__PURE__ */ jsx(Trash, {}),
-              label: t("actions.delete"),
-              onClick: handleDelete
-            }
-          ]
-        }
+        ] : []
       ]
     }
   );
@@ -244,7 +204,6 @@ var ProductListTable = () => {
       /* @__PURE__ */ jsx2(Heading, { level: "h1", children: t("products.domain") }),
       /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-center gap-x-2", children: [
         /* @__PURE__ */ jsx2(Button, { size: "small", variant: "secondary", asChild: true, children: /* @__PURE__ */ jsx2(Link, { to: `export${location.search}`, children: t("actions.export") }) }),
-        /* @__PURE__ */ jsx2(Button, { size: "small", variant: "secondary", asChild: true, children: /* @__PURE__ */ jsx2(Link, { to: `import${location.search}`, children: t("actions.import") }) }),
         /* @__PURE__ */ jsx2(Button, { size: "small", variant: "secondary", asChild: true, children: /* @__PURE__ */ jsx2(Link, { to: "create", children: t("actions.create") }) })
       ] })
     ] }),
@@ -282,6 +241,7 @@ var useColumns = () => {
       ...base,
       columnHelper.display({
         id: "actions",
+        header: () => /* @__PURE__ */ jsx2("span", { className: "sr-only", children: "Actions" }),
         cell: ({ row }) => {
           return /* @__PURE__ */ jsx2(ProductActions, { product: row.original });
         }
@@ -371,7 +331,6 @@ var ConfigurableProductListTable = () => {
         heading: t("products.domain"),
         actions: [
           { label: t("actions.export"), to: `export${location.search}` },
-          { label: t("actions.import"), to: `import${location.search}` },
           { label: t("actions.create"), to: "create" }
         ]
       }
diff --git a/dist/product-variant-detail-UNS5EC7C.mjs b/dist/product-variant-detail-UNS5EC7C.mjs
index 736e26e394ddf9ae9c9e04637cba9f12bde0a01d..e8c799ff5cd5c25a2feabceff2c2c4ea00e3e497 100644
--- a/dist/product-variant-detail-UNS5EC7C.mjs
+++ b/dist/product-variant-detail-UNS5EC7C.mjs
@@ -68,7 +68,6 @@ import "./chunk-SKQPG6BC.mjs";
 import "./chunk-LYZ7OUA3.mjs";
 import "./chunk-D3TDNKSZ.mjs";
 import {
-  useDeleteVariant,
   useProductVariant,
   variantsQueryKeys
 } from "./chunk-TPANFD43.mjs";
@@ -132,36 +131,14 @@ import { CORE_LAYOUT_IDS } from "@medusajs/admin-shared";
 import { useLoaderData, useParams } from "react-router-dom";
 
 // src/routes/product-variants/product-variant-detail/components/variant-general-section/variant-general-section.tsx
-import { Component, GlobeEurope, PencilSquare, Trash } from "@medusajs/icons";
-import { Badge, Container, Heading, usePrompt } from "@medusajs/ui";
+import { Component, GlobeEurope, PencilSquare } from "@medusajs/icons";
+import { Badge, Container, Heading } from "@medusajs/ui";
 import { useTranslation } from "react-i18next";
-import { useNavigate } from "react-router-dom";
 import { jsx as jsx2, jsxs } from "react/jsx-runtime";
 function VariantGeneralSection({ variant }) {
   const { t } = useTranslation();
-  const prompt = usePrompt();
-  const navigate = useNavigate();
   const isTranslationsEnabled = useFeatureFlag("translation");
   const hasInventoryKit = (variant.inventory?.length ?? 0) > 1;
-  const { mutateAsync } = useDeleteVariant(variant.product_id, variant.id);
-  const handleDelete = async () => {
-    const res = await prompt({
-      title: t("general.areYouSure"),
-      description: t("products.variant.deleteWarning", {
-        title: variant.title
-      }),
-      confirmText: t("actions.delete"),
-      cancelText: t("actions.cancel")
-    });
-    if (!res) {
-      return;
-    }
-    await mutateAsync(void 0, {
-      onSuccess: () => {
-        navigate("..", { replace: true });
-      }
-    });
-  };
   return /* @__PURE__ */ jsxs(Container, { className: "divide-y p-0", children: [
     /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between px-6 py-4", children: [
       /* @__PURE__ */ jsxs("div", { children: [
@@ -194,16 +171,7 @@ function VariantGeneralSection({ variant }) {
                   }
                 ]
               }
-            ] : [],
-            {
-              actions: [
-                {
-                  label: t("actions.delete"),
-                  onClick: handleDelete,
-                  icon: /* @__PURE__ */ jsx2(Trash, {})
-                }
-              ]
-            }
+            ] : []
           ]
         }
       ) })
diff --git a/src/components/common/action-menu/action-menu.tsx b/src/components/common/action-menu/action-menu.tsx
index 1c1a8180222bae4279fa60e23703a76c2437316f..5b06cc2c6c87624d164458aa4c4486a542a7684a 100644
--- a/src/components/common/action-menu/action-menu.tsx
+++ b/src/components/common/action-menu/action-menu.tsx
@@ -41,7 +41,7 @@ export const ActionMenu = ({
 }: ActionMenuProps) => {
   const direction = useDocumentDirection()
   const inner = children ?? (
-    <IconButton size="small" variant={variant}>
+    <IconButton aria-label="Open actions" size="small" variant={variant}>
       <EllipsisHorizontal />
     </IconButton>
   )
diff --git a/src/components/filtering/order-by/order-by.tsx b/src/components/filtering/order-by/order-by.tsx
index 20111880b52dc6de18727d963f3c6f274b6c791f..8d4987f622a9e8a884d39b5600e9250b71aeb13c 100644
--- a/src/components/filtering/order-by/order-by.tsx
+++ b/src/components/filtering/order-by/order-by.tsx
@@ -104,7 +104,7 @@ export const OrderBy = ({ keys }: OrderByProps) => {
   return (
     <DropdownMenu dir={direction}>
       <DropdownMenu.Trigger asChild>
-        <IconButton size="small">
+        <IconButton aria-label="Sort results" size="small">
           <ArrowUpDown />
         </IconButton>
       </DropdownMenu.Trigger>
diff --git a/src/components/layout-composer/layout-composer.tsx b/src/components/layout-composer/layout-composer.tsx
index 875804e3e17da3e2e8fe9c3ef4934a58e3c34840..7e551fcee196a05b2c6ef964039c154571f817e2 100644
--- a/src/components/layout-composer/layout-composer.tsx
+++ b/src/components/layout-composer/layout-composer.tsx
@@ -219,26 +219,34 @@ const LayoutComposerRoot = <TLayoutId extends Layouts, TData>({
             layout?.sections?.map((s) => s.id) ?? []
           )
 
-      // Build raw entries (core + widgets) at their natural sections. Relative
-      // placement before any user preference comes purely from the order they
-      // are pushed here, preserved by the stable sort in `buildDisplayEntries`.
-      // Widgets are pushed after core entries so they render after by default —
+      // Build raw entries (before widgets + core + after widgets) at their
+      // natural sections. Relative placement before any user preference comes
+      // from the order they are pushed here, preserved by the stable sort in
+      // `buildDisplayEntries`.
       // If a configuration has been saved, newly added widget or core entry
       // (which has no saved order) surfaces at the top, otherwise it will be
       // placed at the bottom of the section.
-      const rawEntries: RawEntry[] = buildCoreEntries(coreElementsBySection)
+      const beforeWidgets: RawEntry[] = []
+      const afterWidgets: RawEntry[] = []
       for (const [naturalSection, widgets] of Object.entries(naturalWidgets)) {
         for (const w of widgets) {
           const WidgetComponent = w.Component as ComponentType<{
             data?: unknown
           }>
-          rawEntries.push({
+          const entry: RawEntry = {
             widgetId: w.widgetId,
             render: (data) => <WidgetComponent data={data} />,
             naturalSection,
-          })
+          }
+          const target = w.placement === "before" ? beforeWidgets : afterWidgets
+          target.push(entry)
         }
       }
+      const rawEntries: RawEntry[] = [
+        ...beforeWidgets,
+        ...buildCoreEntries(coreElementsBySection),
+        ...afterWidgets,
+      ]
 
       // Apply the active preference (draft when editing, persisted otherwise),
       // keeping hidden entries with `hidden: true` so we can ghost them in edit
diff --git a/src/components/layout-composer/types.ts b/src/components/layout-composer/types.ts
index 46df4afa1bfdfd04a282cf54b40d401aa271365e..4cdbc3376933f41d35e0d21b6b924aff1f20a53c 100644
--- a/src/components/layout-composer/types.ts
+++ b/src/components/layout-composer/types.ts
@@ -36,6 +36,7 @@ export type LayoutPreference = {
 
 export type WidgetRenderEntry = {
   Component: ComponentType
+  placement: "before" | "after"
   widgetId: string
 }
 export type SectionWidgetMap = Record<string, WidgetRenderEntry[]>
diff --git a/src/components/table/data-table/data-table-order-by/data-table-order-by.tsx b/src/components/table/data-table/data-table-order-by/data-table-order-by.tsx
index f446d8287bc2d59fbe31e472f5806337eeb72c77..a30c59aa3b57c5bdaea3d50cd10d848b09a66673 100644
--- a/src/components/table/data-table/data-table-order-by/data-table-order-by.tsx
+++ b/src/components/table/data-table/data-table-order-by/data-table-order-by.tsx
@@ -102,7 +102,7 @@ export const DataTableOrderBy = <TData,>({
   return (
     <DropdownMenu dir={direction}>
       <DropdownMenu.Trigger asChild>
-        <IconButton size="small">
+        <IconButton aria-label="Sort results" size="small">
           <DescendingSorting />
         </IconButton>
       </DropdownMenu.Trigger>
diff --git a/src/components/table/data-table/data-table-root/data-table-root.tsx b/src/components/table/data-table/data-table-root/data-table-root.tsx
index a6ea006960c9098063ccbcfcc8221eda5d49c4e9..cf73f76ca876b0cfc94952ffc8f6fa68dae1d8af 100644
--- a/src/components/table/data-table/data-table-root/data-table-root.tsx
+++ b/src/components/table/data-table/data-table-root/data-table-root.tsx
@@ -256,7 +256,8 @@ export const DataTableRoot = <TData,>({
                       )
 
                       const isTabableLink = isFirstCell && !!to
-                      const shouldRenderAsLink = !!to && !isSelectCell
+                      const shouldRenderAsLink =
+                        !!to && !isSelectCell && cell.column.id !== "actions"
 
                       return (
                         <Table.Cell
diff --git a/src/components/table/data-table/data-table-search/data-table-search.tsx b/src/components/table/data-table/data-table-search/data-table-search.tsx
index a648dbcedd365a4bb72c95f63efe998d17ab92a3..0d52fe6203f2efb9c384f48d8cb76955ae74a04b 100644
--- a/src/components/table/data-table/data-table-search/data-table-search.tsx
+++ b/src/components/table/data-table/data-table-search/data-table-search.tsx
@@ -48,6 +48,7 @@ export const DataTableSearch = ({
 
   return (
     <Input
+      aria-label={placeholderText}
       autoComplete="off"
       name="q"
       type="search"
diff --git a/src/dashboard-app/dashboard-app.tsx b/src/dashboard-app/dashboard-app.tsx
index 5f57c65d6cbc055d0fffec8ab3d289c279a14900..c2b622d9d9ec03eeee9f2b592087dba70f9b16ee 100644
--- a/src/dashboard-app/dashboard-app.tsx
+++ b/src/dashboard-app/dashboard-app.tsx
@@ -467,9 +467,10 @@ export class DashboardApp {
 
   /**
    * Returns widgets for a given base zone grouped by layout section.
-   * The legacy `.before`/`.after` ordering suffixes are stripped and
-   * ignored. Widgets are returned inregistration order and will be
-   * assigned a default order of 0 in the LayoutComposer.
+   * The legacy `.before`/`.after` ordering suffixes define the widget's
+   * natural placement around core entries. Widgets are returned in
+   * registration order and will be assigned a default order of 0 in the
+   * LayoutComposer.
    */
   private getWidgetsForSections(
     route: string,
@@ -482,9 +483,9 @@ export class DashboardApp {
         continue
       }
 
-      const suffix = zone
-        .slice(route.length + 1)
-        .replace(/\.?(before|after)$/, "")
+      const zoneSuffix = zone.slice(route.length + 1)
+      const placement = zoneSuffix.endsWith("before") ? "before" : "after"
+      const suffix = zoneSuffix.replace(/\.?(before|after)$/, "")
 
       const mainSection = sections.includes("main") ? "main" : sections[0]
       const section = suffix || mainSection
@@ -500,6 +501,7 @@ export class DashboardApp {
         const sourceId = ext.widgetId ?? `${zone}:${i}`
         result[section].push({
           Component: ext.Component,
+          placement,
           // The id intentionally omits the section so a widget keeps its
           // identity (and saved preference) when its zone moves to a different
           // section of the same route.
diff --git a/src/dashboard-app/routes/get-route.map.tsx b/src/dashboard-app/routes/get-route.map.tsx
index a2b3dc5ddea120bab6c99eb40b3d0c3c068285de..8284a7ad41530ba115e8b5b6cbf4a79522fef5f8 100644
--- a/src/dashboard-app/routes/get-route.map.tsx
+++ b/src/dashboard-app/routes/get-route.map.tsx
@@ -39,16 +39,6 @@ export function getRouteMap({
                   path: "",
                   lazy: () => import("../../routes/products/product-list"),
                   children: [
-                    {
-                      path: "create",
-                      lazy: () =>
-                        import("../../routes/products/product-create"),
-                    },
-                    {
-                      path: "import",
-                      lazy: () =>
-                        import("../../routes/products/product-import"),
-                    },
                     {
                       path: "export",
                       lazy: () =>
diff --git a/src/routes/product-variants/product-variant-detail/components/variant-general-section/variant-general-section.tsx b/src/routes/product-variants/product-variant-detail/components/variant-general-section/variant-general-section.tsx
index 4f02029ff0cb1378fc9be20fc5495b98dd2c5729..2fd28c4bddf552aa38e5fbade8b9b556606d762c 100644
--- a/src/routes/product-variants/product-variant-detail/components/variant-general-section/variant-general-section.tsx
+++ b/src/routes/product-variants/product-variant-detail/components/variant-general-section/variant-general-section.tsx
@@ -1,11 +1,9 @@
-import { Component, GlobeEurope, PencilSquare, Trash } from "@medusajs/icons"
-import { Badge, Container, Heading, usePrompt } from "@medusajs/ui"
+import { Component, GlobeEurope, PencilSquare } from "@medusajs/icons"
+import { Badge, Container, Heading } from "@medusajs/ui"
 import { useTranslation } from "react-i18next"
-import { useNavigate } from "react-router-dom"
 
 import { ActionMenu } from "../../../../../components/common/action-menu"
 import { SectionRow } from "../../../../../components/common/section"
-import { useDeleteVariant } from "../../../../../hooks/api/products"
 import { useFeatureFlag } from "../../../../../providers/feature-flag-provider"
 import { ExtendedVariant } from "../../constants"
 
@@ -15,35 +13,10 @@ type VariantGeneralSectionProps = {
 
 export function VariantGeneralSection({ variant }: VariantGeneralSectionProps) {
   const { t } = useTranslation()
-  const prompt = usePrompt()
-  const navigate = useNavigate()
   const isTranslationsEnabled = useFeatureFlag("translation")
 
   const hasInventoryKit = (variant.inventory?.length ?? 0) > 1
 
-  const { mutateAsync } = useDeleteVariant(variant.product_id!, variant.id)
-
-  const handleDelete = async () => {
-    const res = await prompt({
-      title: t("general.areYouSure"),
-      description: t("products.variant.deleteWarning", {
-        title: variant.title,
-      }),
-      confirmText: t("actions.delete"),
-      cancelText: t("actions.cancel"),
-    })
-
-    if (!res) {
-      return
-    }
-
-    await mutateAsync(undefined, {
-      onSuccess: () => {
-        navigate("..", { replace: true })
-      },
-    })
-  }
-
   return (
     <Container className="divide-y p-0">
       <div className="flex items-center justify-between px-6 py-4">
@@ -85,15 +58,6 @@ export function VariantGeneralSection({ variant }: VariantGeneralSectionProps) {
                     },
                   ]
                 : []),
-              {
-                actions: [
-                  {
-                    label: t("actions.delete"),
-                    onClick: handleDelete,
-                    icon: <Trash />,
-                  },
-                ],
-              },
             ]}
           />
         </div>
diff --git a/src/routes/products/product-detail/components/product-general-section/product-general-section.tsx b/src/routes/products/product-detail/components/product-general-section/product-general-section.tsx
index 17b276e2e45db3b8c891226c0a6ee77913c43f20..c0d74da288f74e761737cbe28d02b6c06b98707a 100644
--- a/src/routes/products/product-detail/components/product-general-section/product-general-section.tsx
+++ b/src/routes/products/product-detail/components/product-general-section/product-general-section.tsx
@@ -1,12 +1,10 @@
-import { GlobeEurope, PencilSquare, Trash } from "@medusajs/icons"
+import { GlobeEurope, PencilSquare } from "@medusajs/icons"
 import { HttpTypes } from "@medusajs/types"
-import { Container, Heading, StatusBadge, toast, usePrompt } from "@medusajs/ui"
+import { Container, Heading, StatusBadge } from "@medusajs/ui"
 import { useTranslation } from "react-i18next"
-import { useNavigate } from "react-router-dom"
 
 import { ActionMenu } from "../../../../../components/common/action-menu"
 import { SectionRow } from "../../../../../components/common/section"
-import { useDeleteProduct } from "../../../../../hooks/api/products"
 import { useExtension } from "../../../../../providers/extension-provider"
 import { useFeatureFlag } from "../../../../../providers/feature-flag-provider"
 
@@ -33,41 +31,11 @@ export const ProductGeneralSection = ({
   product,
 }: ProductGeneralSectionProps) => {
   const { t } = useTranslation()
-  const prompt = usePrompt()
-  const navigate = useNavigate()
   const { getDisplays } = useExtension()
   const isTranslationsEnabled = useFeatureFlag("translation")
 
   const displays = getDisplays("product", "general")
 
-  const { mutateAsync } = useDeleteProduct(product.id)
-
-  const handleDelete = async () => {
-    const res = await prompt({
-      title: t("general.areYouSure"),
-      description: t("products.deleteWarning", {
-        title: product.title,
-      }),
-      confirmText: t("actions.delete"),
-      cancelText: t("actions.cancel"),
-    })
-
-    if (!res) {
-      return
-    }
-
-    await mutateAsync(undefined, {
-      onSuccess: () => {
-        navigate("..")
-      },
-      onError: (e) => {
-        toast.error(t("products.toasts.delete.error.header"), {
-          description: e.message,
-        })
-      },
-    })
-  }
-
   return (
     <Container className="divide-y p-0">
       <div className="flex items-center justify-between px-6 py-4">
@@ -100,15 +68,6 @@ export const ProductGeneralSection = ({
                     },
                   ]
                 : []),
-              {
-                actions: [
-                  {
-                    label: t("actions.delete"),
-                    onClick: handleDelete,
-                    icon: <Trash />,
-                  },
-                ],
-              },
             ]}
           />
         </div>
diff --git a/src/routes/products/product-detail/components/product-variant-section/product-variant-section.tsx b/src/routes/products/product-detail/components/product-variant-section/product-variant-section.tsx
index e513df378437acb4bc5432847a242f1bb9c4bdf7..377b4d9cb3b401ebeecbbc3fca651c52f55b8ac1 100644
--- a/src/routes/products/product-detail/components/product-variant-section/product-variant-section.tsx
+++ b/src/routes/products/product-detail/components/product-variant-section/product-variant-section.tsx
@@ -3,7 +3,6 @@ import {
   Component,
   GlobeEurope,
   PencilSquare,
-  Trash,
 } from "@medusajs/icons"
 import { HttpTypes } from "@medusajs/types"
 import {
@@ -15,7 +14,6 @@ import {
   createDataTableFilterHelper,
   DataTableAction,
   Tooltip,
-  usePrompt,
 } from "@medusajs/ui"
 import { keepPreviousData } from "@tanstack/react-query"
 import { useCallback, useMemo } from "react"
@@ -25,10 +23,7 @@ import { CellContext } from "@tanstack/react-table"
 import { useNavigate, useSearchParams } from "react-router-dom"
 import { DataTable } from "../../../../../components/data-table"
 import { useDataTableDateFilters } from "../../../../../components/data-table/helpers/general/use-data-table-date-filters"
-import {
-  useDeleteVariantLazy,
-  useProductVariants,
-} from "../../../../../hooks/api/products"
+import { useProductVariants } from "../../../../../hooks/api/products"
 import { useQueryParams } from "../../../../../hooks/use-query-params"
 import { PRODUCT_VARIANT_IDS_KEY } from "../../../common/constants"
 import { Thumbnail } from "../../../../../components/common/thumbnail"
@@ -154,8 +149,6 @@ const columnHelper =
 const useColumns = (product: HttpTypes.AdminProduct) => {
   const { t } = useTranslation()
   const navigate = useNavigate()
-  const { mutateAsync } = useDeleteVariantLazy(product.id)
-  const prompt = usePrompt()
   const [searchParams] = useSearchParams()
 
   const tableSearchParams = useMemo(() => {
@@ -168,26 +161,6 @@ const useColumns = (product: HttpTypes.AdminProduct) => {
     return filtered
   }, [searchParams])
 
-  const handleDelete = useCallback(
-    async (id: string, title: string) => {
-      const res = await prompt({
-        title: t("general.areYouSure"),
-        description: t("products.deleteVariantWarning", {
-          title,
-        }),
-        confirmText: t("actions.delete"),
-        cancelText: t("actions.cancel"),
-      })
-
-      if (!res) {
-        return
-      }
-
-      await mutateAsync({ variantId: id })
-    },
-    [mutateAsync, prompt, t]
-  )
-
   const optionColumns = useMemo(() => {
     if (!product?.options) {
       return []
@@ -258,15 +231,6 @@ const useColumns = (product: HttpTypes.AdminProduct) => {
         },
       ]
 
-      const secondaryActions: DataTableAction<HttpTypes.AdminProductVariant>[] =
-        [
-          {
-            icon: <Trash />,
-            label: t("actions.delete"),
-            onClick: () => handleDelete(variant.id, variant.title!),
-          },
-        ]
-
       const inventoryItemsCount = variant.inventory_items?.length || 0
 
       switch (inventoryItemsCount) {
@@ -307,9 +271,9 @@ const useColumns = (product: HttpTypes.AdminProduct) => {
         }
       }
 
-      return [mainActions, secondaryActions]
+      return [mainActions]
     },
-    [handleDelete, navigate, t, tableSearchParams]
+    [navigate, t, tableSearchParams]
   )
 
   const getInventory = useCallback(
diff --git a/src/routes/products/product-list/components/product-list-table/configurable-product-list-table.tsx b/src/routes/products/product-list/components/product-list-table/configurable-product-list-table.tsx
index a314648b4d67f3de565d2dbb5b7d4af1cefd1d7d..65a010a7583038483df718f8d32567fde4c3e84b 100644
--- a/src/routes/products/product-list/components/product-list-table/configurable-product-list-table.tsx
+++ b/src/routes/products/product-list/components/product-list-table/configurable-product-list-table.tsx
@@ -16,7 +16,6 @@ export const ConfigurableProductListTable = () => {
         heading={t("products.domain")}
         actions={[
           { label: t("actions.export"), to: `export${location.search}` },
-          { label: t("actions.import"), to: `import${location.search}` },
           { label: t("actions.create"), to: "create" },
         ]}
       />
diff --git a/src/routes/products/product-list/components/product-list-table/product-list-table-actions.tsx b/src/routes/products/product-list/components/product-list-table/product-list-table-actions.tsx
index 407eb26cb0ab6b39d0be1d7bc7056a279c19e7eb..7ad03dc34067bf714089bea4e56fbf34e2993ad9 100644
--- a/src/routes/products/product-list/components/product-list-table/product-list-table-actions.tsx
+++ b/src/routes/products/product-list/components/product-list-table/product-list-table-actions.tsx
@@ -1,10 +1,8 @@
-import { GlobeEurope, PencilSquare, Trash } from "@medusajs/icons"
+import { GlobeEurope, PencilSquare } from "@medusajs/icons"
 import { HttpTypes } from "@medusajs/types"
-import { toast, usePrompt } from "@medusajs/ui"
 import { useTranslation } from "react-i18next"
 
 import { ActionMenu } from "../../../../../components/common/action-menu"
-import { useDeleteProduct } from "../../../../../hooks/api/products"
 import { useFeatureFlag } from "../../../../../providers/feature-flag-provider"
 
 export const ProductActions = ({
@@ -13,40 +11,8 @@ export const ProductActions = ({
   product: HttpTypes.AdminProduct
 }) => {
   const { t } = useTranslation()
-  const prompt = usePrompt()
-  const { mutateAsync } = useDeleteProduct(product.id)
   const isTranslationsEnabled = useFeatureFlag("translation")
 
-  const handleDelete = async () => {
-    const res = await prompt({
-      title: t("general.areYouSure"),
-      description: t("products.deleteWarning", {
-        title: product.title,
-      }),
-      confirmText: t("actions.delete"),
-      cancelText: t("actions.cancel"),
-    })
-
-    if (!res) {
-      return
-    }
-
-    await mutateAsync(undefined, {
-      onSuccess: () => {
-        toast.success(t("products.toasts.delete.success.header"), {
-          description: t("products.toasts.delete.success.description", {
-            title: product.title,
-          }),
-        })
-      },
-      onError: (e) => {
-        toast.error(t("products.toasts.delete.error.header"), {
-          description: e.message,
-        })
-      },
-    })
-  }
-
   return (
     <ActionMenu
       groups={[
@@ -72,15 +38,6 @@ export const ProductActions = ({
               },
             ]
           : []),
-        {
-          actions: [
-            {
-              icon: <Trash />,
-              label: t("actions.delete"),
-              onClick: handleDelete,
-            },
-          ],
-        },
       ]}
     />
   )
diff --git a/src/routes/products/product-list/components/product-list-table/product-list-table.tsx b/src/routes/products/product-list/components/product-list-table/product-list-table.tsx
index 0d9c1b8b2602ef58fbfe66dc2a208f05622b092a..6adaac98b63fdbf5c26f816141fe0c54c21a1997 100644
--- a/src/routes/products/product-list/components/product-list-table/product-list-table.tsx
+++ b/src/routes/products/product-list/components/product-list-table/product-list-table.tsx
@@ -61,9 +61,6 @@ export const ProductListTable = () => {
           <Button size="small" variant="secondary" asChild>
             <Link to={`export${location.search}`}>{t("actions.export")}</Link>
           </Button>
-          <Button size="small" variant="secondary" asChild>
-            <Link to={`import${location.search}`}>{t("actions.import")}</Link>
-          </Button>
           <Button size="small" variant="secondary" asChild>
             <Link to="create">{t("actions.create")}</Link>
           </Button>
@@ -104,6 +101,7 @@ const useColumns = () => {
       ...base,
       columnHelper.display({
         id: "actions",
+        header: () => <span className="sr-only">Actions</span>,
         cell: ({ row }) => {
           return <ProductActions product={row.original} />
         },
